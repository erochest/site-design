# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'sass', :input => 'sass', :output => 'css'

guard 'livereload' do
  watch(%r{.*\.html})
  watch(%r{css/.*\.css})
  watch(%r{js/.*\.js})
end
